### Automount MTP Devices on OS X ###

A simple background process for automounting MTP devices on OS X via simple-mtpfs and make them show up in the Finder sidebar.

#### Installation ####

	make install
